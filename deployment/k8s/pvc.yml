apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: toggler-redis-datasource-volume-claim
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
