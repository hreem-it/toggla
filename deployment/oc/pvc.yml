apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: toggler-redis-datasource-volume-claim
  labels:
    app.kubernetes.io/part-of: toggler
spec:
  storageClassName: vsan
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
